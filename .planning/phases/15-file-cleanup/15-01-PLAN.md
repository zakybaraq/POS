---
phase: 15-file-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - CONTEXT.md
  - login-page-snapshot.md
  - login-snapshot.md
  - login-after-submit.md
  - PR_BODY.md
  - issue.md
  - LOYALTY_IMPLEMENTATION.md
autonomous: true
requirements:
  - REQ-001

must_haves:
  truths:
    - "All 6 temporary .md files deleted from repository root"
    - "CONTEXT.md moved to .planning/archive/ with version suffix"
    - ".gitignore updated to exclude test results and log files"
    - "Git status shows clean working directory after cleanup"
    - "All 122+ tests still passing"
  artifacts:
    - path: ".planning/archive/CONTEXT-v1.0.md"
      provides: "Archived project context from v1.0"
    - path: ".gitignore"
      provides: "Updated ignore patterns"
      contains: ["test-results/", "playwright-report/", "*.log"]
  key_links:
    - from: "root directory"
      to: ".planning/archive/"
      via: "mv CONTEXT.md"
      pattern: "archive old docs"
---

<objective>
Clean up temporary files and improve repository organization for v2.2 codebase refactoring.

Purpose: Remove clutter before major restructuring. Eliminate temporary files, auto-generated directories, and outdated documentation from the repository root.

Output: Clean repository root with only permanent files, archived old context, updated .gitignore
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/15-CONTEXT.md
@.planning/ROADMAP.md
@.gitignore
@CONTEXT.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Create archive directory</name>
<files>.planning/archive/</files>
<action>
Create .planning/archive/ directory to store historical documentation.

This directory will hold archived context files from previous milestones.
</action>
<verify>
Directory exists
ls -la .planning/archive/
</verify>
<done>.planning/archive/ directory created</done>
</task>

<task type="auto">
<name>Task 2: Archive CONTEXT.md</name>
<files>CONTEXT.md, .planning/archive/CONTEXT-v1.0.md</files>
<action>
Move CONTEXT.md from repository root to .planning/archive/CONTEXT-v1.0.md

This file contains the old project status from v1.0 milestone. Preserving it in archive for reference.
</action>
<verify>
File moved and exists in archive
ls .planning/archive/CONTEXT-v1.0.md && test ! -f CONTEXT.md
</verify>
<done>CONTEXT.md archived as .planning/archive/CONTEXT-v1.0.md</done>
</task>

<task type="auto">
<name>Task 3: Delete temporary markdown files</name>
<files>login-page-snapshot.md, login-snapshot.md, login-after-submit.md, PR_BODY.md, issue.md, LOYALTY_IMPLEMENTATION.md</files>
<action>
Delete 6 temporary markdown files from repository root:

1. login-page-snapshot.md - Temporary Playwright test snapshot
2. login-snapshot.md - Temporary Playwright test snapshot
3. login-after-submit.md - Temporary Playwright test snapshot
4. PR_BODY.md - One-time pull request template
5. issue.md - One-time issue template
6. LOYALTY_IMPLEMENTATION.md - Unused implementation document

All files are temporary/single-use and safe to delete (git history preserves if needed).
</action>
<verify>
Files deleted
for file in login-page-snapshot.md login-snapshot.md login-after-submit.md PR_BODY.md issue.md LOYALTY_IMPLEMENTATION.md; do test ! -f "$file" && echo "Deleted: $file"; done
</verify>
<done>All 6 temporary files deleted from root</done>
</task>

<task type="auto">
<name>Task 4: Update .gitignore</name>
<files>.gitignore</files>
<action>
Add patterns to .gitignore for test results and log files:

Append these patterns to existing .gitignore:

```
# Test results
test-results/
playwright-report/

# Logs
*.log
*.log.*

# OS files
Thumbs.db
```

Keep existing patterns (node_modules, .env, etc.).
</action>
<verify>
Patterns added
grep -E "^test-results/|^playwright-report/|^\*\.log" .gitignore
</verify>
<done>.gitignore updated with test-results/, playwright-report/, *.log patterns</done>
</task>

<task type="auto">
<name>Task 5: Verify git status</name>
<files>.</files>
<action>
Verify git status shows expected changes:

Expected:
- Deleted: login-page-snapshot.md, login-snapshot.md, login-after-submit.md
- Deleted: PR_BODY.md, issue.md, LOYALTY_IMPLEMENTATION.md
- Deleted: CONTEXT.md (moved)
- Modified: .gitignore
- Untracked: .planning/archive/ (new directory)

Run: git status
</action>
<verify>
Check git status output
git status --short | grep -E "^[DAM]" | wc -l
</verify>
<done>Git status shows only expected cleanup changes</done>
</task>

<task type="auto">
<name>Task 6: Run tests to verify no regression</name>
<files>.</files>
<action>
Run test suite to ensure cleanup doesn't break anything:

```
bun test
```

Expected: All tests pass (122+ passing, no new failures)
</action>
<verify>
Tests pass
bun test 2>&1 | tail -5
</verify>
<done>All tests passing, no functional impact from cleanup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | Local file operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | file deletion | accept | All files backed up in git history |
| T-15-02 | Information Disclosure | file permissions | accept | Standard git permissions apply |
</threat_model>

<verification>
- [ ] .planning/archive/ directory exists
- [ ] CONTEXT.md archived as CONTEXT-v1.0.md
- [ ] All 6 temporary .md files deleted from root
- [ ] .gitignore includes test-results/, playwright-report/, *.log
- [ ] Git status clean (only expected changes)
- [ ] All tests passing
</verification>

<success_criteria>
- Repository root contains only permanent project files
- No temporary .md files in root directory
- Old CONTEXT.md preserved in archive
- .gitignore prevents test results and logs from being committed
- All existing tests still pass
- Clean git status ready for commit
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-cleanup/15-01-SUMMARY.md`
</output>
