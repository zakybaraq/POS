---
phase: 12-purchase-order-system
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/db/schema.ts
  - src/repositories/supplier.ts
  - src/routes/purchase-orders.ts
autonomous: false
requirements:
  - REQ-002
user_setup: []

must_haves:
  truths:
    - "PO approval workflow: draft → submitted → approved/rejected → received"
    - "Rejected POs can be resubmitted"
    - "Approval requires admin role"
  artifacts:
    - path: src/db/schema.ts
      provides: Status enum includes submitted, approved, rejected
      contains: mysqlEnum.*status.*submitted.*approved.*rejected
    - path: src/routes/purchase-orders.ts
      provides: POST /:id/approve and POST /:id/reject endpoints
  key_links:
    - from: POST /:id/approve
      to: updatePOStatus
      via: status = 'approved'
---

<objective>
Enhance PO system with proper approval workflow

Purpose: Add approval gate for purchasing workflow
Output: Full workflow with admin approval (draft → submitted → approved → received)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-purchase-order-system/12-01-PLAN.md
@.planning/12-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Plan 12-01 verification</name>
  <what-built>The pre-implemented PO infrastructure from prior session</what-built>
  <how-to-verify>
Run the test suite to verify all PO functionality:
1. bun test test/integration/purchase-order.test.ts
2. Expected: 90 pass (3 pre-existing WebSocket failures)
3. Check GET /api/purchase-orders returns valid response
  </how-to-verify>
  <resume-signal>
Type "approved" or describe issues
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update PO status enum</name>
  <files>
    - src/db/schema.ts
  </files>
  <action>
Update purchaseOrders status enum to include full workflow:

Current (simplified):
  status: mysqlEnum('status', ['draft', 'ordered', 'received', 'cancelled'])

Replace with full workflow:
  status: mysqlEnum('status', ['draft', 'submitted', 'approved', 'rejected', 'received', 'cancelled'])

After update, run: npx drizzle-kit push
  </action>
  <verify>
<automated>grep -E "mysqlEnum\('status', \['\"]src/db/schema.ts | grep -c "submitted"</automated>
  </verify>
  <done>
PO status includes submitted, approved, rejected states
</done>
</task>

<task type="auto">
  <name>Task 3: Add approval endpoints</name>
  <files>
    - src/routes/purchase-orders.ts
    - src/repositories/supplier.ts
  </files>
  <action>
Add to src/routes/purchase-orders.ts:
- POST /:id/approve - Approve PO (admin only), transitions: submitted → approved
- POST /:id/reject - Reject PO (admin only), transitions: submitted → rejected

Add to src/repositories/supplier.ts:
- approvePO(id, approvedBy) - Mark PO as approved
- rejectPO(id, rejectedBy) - Mark PO as rejected

The approve endpoint should:
1. Verify current status is 'submitted'
2. Check user has admin role
3. Update status to 'approved'
4. Send notification
  </action>
  <verify>
<automated>grep -E "(approve|reject)PO" src/routes/purchase-orders.ts</automated>
  </verify>
  <done>
Approval endpoints exist
</done>
</task>

<task type="auto">
  <name>Task 4: Update status transition rules</name>
  <files>
    - src/repositories/supplier.ts
  </files>
  <action>
Update updatePOStatus function to enforce valid transitions:

Valid transitions:
- draft → submitted (submit for approval)
- submitted → approved (admin approves)
- submitted → rejected (admin rejects)
- approved → received (receive goods)
- approved → cancelled (cancel)
- draft → cancelled (cancel without approval)

Invalid transitions should return error.
  </action>
  <verify>
<automated>grep "switch.*status" src/repositories/supplier.ts</automated>
  </verify>
  <done>
Status transitions are validated
</done>
</task>

<task type="auto">
  <name>Task 5: Run tests</name>
  <files>
    - test/integration/purchase-order.test.ts
  </files>
  <action>
Update tests for new workflow, then run:
  bun test test/integration/purchase-order.test.ts
  </action>
  <verify>
<automated>bun test test/integration/purchase-order.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
90+ tests passing (new approval tests included)
</done>
</task>

</tasks>

<verification>
New endpoints functional:
- POST /api/purchase-orders/:id/approve 
- POST /api/purchase-orders/:id/reject 
- Invalid transitions rejected

Tests pass: 90+
</verification>

<success_criteria>
- [x] PO can be submitted for approval
- [x] Admin can approve/reject PO
- [x] Rejected PO can be resubmitted
- [x] Full workflow: draft → submitted → approved → received
</success_criteria>

<output>
After completion, create .planning/phases/12-purchase-order-system/12-02-SUMMARY.md
</output>