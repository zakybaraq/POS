---
phase: 09-dashboard-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard.ts
  - src/routes/dashboard.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - User can see hourly sales trend chart on dashboard
    - User can see top items bar chart on dashboard
    - User can filter data by date range (Today, Yesterday, Last 7 Days, Custom)
    - Charts display real data from API
    - Date filter updates all dashboard widgets when changed
  artifacts:
    - src/pages/dashboard.ts - contains renderHourlySalesChart and renderTopItemsBarChart functions
    - src/pages/dashboard.ts - contains date filter dropdown with 4 options
    - src/routes/dashboard.ts - accepts range query parameter
  key_links:
    - Dashboard page → /api/dashboard/metrics (fetch chart data)
    - Date filter → /api/dashboard/metrics?range= (filter data)
    - Charts → hourly sales data from API response
---

<objective>
Add hourly sales trend chart, top items bar chart, and date range filter to the dashboard.
</objective>

<context>
@/Users/zakybaraq/Desktop/pos/.planning/phases/09-dashboard-frontend/09-CONTEXT.md
@/Users/zakybaraq/Desktop/pos/.planning/8-CONTEXT.md
@/Users/zakybaraq/Desktop/pos/src/pages/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Add hourly sales trend SVG chart to dashboard</name>
  <read_first>
    - src/pages/dashboard.ts (existing dashboard implementation)
    - .planning/phases/09-dashboard-frontend/09-CONTEXT.md (decisions: inline SVG, no external library)
  </read_first>
  <action>
Add inline SVG hourly sales trend chart to dashboard page. Create `renderHourlySalesChart(data)` function that:
1. Takes hourly sales data array (24 hours, values in rupiah)
2. Returns SVG string with line chart
3. Uses CSS variables for colors (var(--color-primary), var(--color-bg-alt))
4. Shows Y-axis with formatted currency values (Rp 100K, Rp 200K, etc.)
5. Shows X-axis with hour labels (00:00, 06:00, 12:00, 18:00)
6. Adds hover tooltip showing exact values on mouseover

Add chart container div after stats-grid with:
- Title: "Tren Penjualan per Jam"
- Width: 100%, height: 200px
- Chart renders inside

Fetch hourly data from existing endpoint (or create /api/dashboard/hourly endpoint if needed).
  </action>
  <verify>
    <automated>grep -l "renderHourlySalesChart" src/pages/dashboard.ts</automated>
  </verify>
  <done>
    - Hourly sales chart renders on dashboard
    - Chart displays 24-hour data points
    - Y-axis shows formatted currency
    - X-axis shows hour labels
  </done>
</task>

<task type="auto">
  <name>Add top items bar chart to dashboard</name>
  <read_first>
    - src/pages/dashboard.ts (existing top items list section)
  </read_first>
  <action>
Add inline SVG horizontal bar chart for "Menu Terlaris Hari Ini". Create `renderTopItemsBarChart(data)` function that:
1. Takes top menus array (name, totalSold, revenue)
2. Returns SVG string with horizontal bar chart
3. Shows top 5 items as bars
4. Bar width proportional to totalSold
5. Shows item name and quantity on each bar
6. Uses accent colors: first bar gold (#f59e0b), second silver (#9ca3af), third bronze (#cd7f32)

Replace existing text-based top items list with this chart in the "Menu Terlaris Hari Ini" card.
  </action>
  <verify>
    <automated>grep -l "renderTopItemsBarChart" src/pages/dashboard.ts</automated>
  </verify>
  <done>
    - Top items bar chart renders on dashboard
    - Shows top 5 items
    - Visual bar representation with item names
  </done>
</task>

<task type="auto">
  <name>Add date range selector to dashboard</name>
  <read_first>
    - src/pages/dashboard.ts
    - src/routes/dashboard.ts (if exists, or src/routes folder structure)
  </read_first>
  <action>
Add date filter dropdown in dashboard header area (after greeting, before stats-grid). Options:
1. "Hari Ini" (Today) - default, shows today's data
2. "Kemarin" (Yesterday) - shows yesterday's data
3. "7 Hari Terakhir" (Last 7 Days) - shows last week aggregate
4. "Pilih Tanggal" (Custom) - opens date picker for range

Create date filter UI:
- Select dropdown with Indonesian labels
- When changed, triggers data refresh for all widgets
- Shows loading indicator during fetch

Add JavaScript handler that:
1. Listens for select change event
2. If "Pilih Tanggal" selected, shows two date inputs (start, end)
3. Makes API call with date range parameters
4. Updates all widgets (sales, orders, charts) with new data

Example endpoint call:
```javascript
const params = new URLSearchParams({ range: selectedRange, startDate, endDate });
const response = await fetch(`/api/dashboard/metrics?${params}`);
```
  </action>
  <verify>
    <automated>grep -E "(dateFilter|Hari Ini|Kemarin|range)" src/pages/dashboard.ts | head -5</automated>
  </verify>
  <done>
    - Date dropdown appears in dashboard header
    - Four options available (Today, Yesterday, Last 7 Days, Custom)
    - Selecting option updates all widgets with filtered data
  </done>
</task>

<task type="auto">
  <name>Add date range API endpoints for dashboard</name>
  <files>src/routes/dashboard.ts (create if not exists)</files>
  <read_first>
    - src/routes folder to understand existing route patterns
    - src/services/dashboard.ts (if exists from Phase 8)
  </read_first>
  <action>
Create or update dashboard route file with date range endpoints:

1. GET /api/dashboard/metrics - Add query params:
   - range: "today" | "yesterday" | "7days" | "custom"
   - startDate: ISO date string (for custom)
   - endDate: ISO date string (for custom)

2. Modify existing metrics query to respect date range:
   - today: WHERE DATE(createdAt) = CURDATE()
   - yesterday: WHERE DATE(createdAt) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
   - 7days: WHERE createdAt >= DATE_SUB(NOW(), INTERVAL 7 DAY)
   - custom: WHERE createdAt BETWEEN startDate AND endDate

3. Return appropriate data for each range:
   - Today/Yesterday: hourly breakdown for chart
   - 7 days: daily totals for each day
   - Custom: daily totals for selected range
  </action>
  <verify>
    <automated>ls src/routes/dashboard.ts 2>/dev/null && grep -l "range" src/routes/dashboard.ts</automated>
  </verify>
  <done>
    - /api/dashboard/metrics accepts range parameter
    - Returns correct data for each date range option
    - Hourly data available for today/yesterday charts
  </done>
</task>

</tasks>

<verification>
- Dashboard page loads without errors
- Both charts render with sample data
- Date filter updates all widgets when changed
- No LSP errors in modified files
</verification>

<success_criteria>
- Hourly sales chart visible and displays data
- Top items bar chart visible and displays data
- Date range filter works for all 4 options
- Real-time updates via WebSocket still functional
- Existing dashboard features preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-frontend/09-01-SUMMARY.md`
</output>