---
phase: 01-improve-ui-halaman-kategori
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/categories.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Halaman kategori terlihat sama dengan halaman pesanan/menu"
    - "Search functionality bekerja"
    - "Modal tambah/edit kategori berfungsi"
  artifacts:
    - path: src/pages/categories.ts
      provides: UI halaman kategori yang seragam
      min_lines: 300
      contains: card, menu-toolbar, table-container, modal
  key_links:
    - from: categories.ts
      to: "/kategori"
      via: Elysia route
      pattern: "get.*kategori"
---

<objective>
Make UI halaman kategori match dengan pattern halaman pesanan/menu

Purpose: Konsisten tampilan antar halaman admin
Output: categories.ts dengan UI pattern lengkap
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/1-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/pages/categories.ts
@src/pages/orders.ts
@src/pages/menu.ts

# Referensi pattern dari orders.ts (lines 46-60):
- `.orders-toolbar` dengan `.orders-toolbar-left` dan `.orders-toolbar-right`
- Search input dengan emoji 🔍 dan placeholder yang sesuai
- Filter dropdown untuk status
- Tombol Export CSV dan Refresh

# Referensi pattern dari menu.ts (lines 75-82):
- `.menu-toolbar` dengan filter dropdowns
- Category dan status filters

</context>

<tasks>

<task type="auto">
  <name>Update toolbar pattern match orders page</name>
  <files>src/pages/categories.ts</files>
  <read_first>src/pages/categories.ts (lines 55-70)</read_first>
  <action>
Update `.menu-toolbar` dalam categories.ts untuk match dengan toolbar di orders.ts:

1. Change `.menu-toolbar-left` div to `.menu-toolbar-left` div (already correct, just verify)
2. Update search input:
   - Add 🔍 emoji di placeholder: `placeholder="🔍 Cari kategori..."`
   - Add class `.menu-search-input` (already exists)
3. In `.menu-toolbar-right`:
   - Keep existing "+ Tambah Kategori" button
   - Optionally add refresh button: `<button class="btn btn-secondary" onclick="loadData()">🔄 Refresh</button>`
</action>
  <acceptance_criteria>
- [ ] Search input has 🔍 emoji in placeholder
- [ ] Toolbar has left and right sections
- [ ] Add Category button present
</acceptance_criteria>
  <verify>
grep -o 'placeholder="🔍 Cari kategori"' src/pages/categories.ts && echo "PASS"
  </verify>
  <done>Search input has 🔍 emoji, toolbar structure matches orders.ts pattern</done>
</task>

<task type="auto">
  <name>Verify modal structure matches spec</name>
  <files>src/pages/categories.ts</files>
  <read_first>src/pages/categories.ts (lines 88-115)</read_first>
  <action>
Verify modal structure matches spec from issue.md:

1. Modal container: `<div class="modal" id="category-modal">`
2. Modal backdrop: `<div class="modal-backdrop" id="category-modal-backdrop">`
3. Modal content: `<div class="modal-content" style="max-width: 400px;">`
4. Modal header with: `<h3>` title + `<button class="modal-close">`
5. Modal body: form with inputs
6. Modal footer: submit button

Already implemented - verify consistency with orders.ts modal (lines 53-66 in issue.md)
</action>
  <acceptance_criteria>
- [ ] Modal has backdrop
- [ ] Modal has max-width 400px
- [ ] Modal has close button with .modal-close class
- [ ] Form has category-name input
- [ ] Submit button in footer
</acceptance_criteria>
  <verify>
grep -c 'modal-backdrop\|modal-content\|modal-close' src/pages/categories.ts
  </verify>
  <done>Modal structure verified - matches spec</done>
</task>

<task type="auto">
  <name>Test search functionality</name>
  <files>src/pages/categories.ts</files>
  <read_first>src/pages/categories.ts (lines 130-180)</read_first>
  <action>
Verify search/filter JavaScript functions exist and work:

1. `filterCategories()` function - called on search input oninput
2. Function filters `currentCategories` array based on search term
3. Calls `renderCategories()` after filtering

Already implemented - verify:
- Search input has `oninput="filterCategories()"` (line 59)
- Filter function filters by name
</action>
  <acceptance_criteria>
- [ ] Search input triggers filterCategories on input
- [ ] filterCategories function filters currentCategories
- [ ] renderCategories called after filter
</acceptance_criteria>
  <verify>
grep -c 'filterCategories\|renderCategories' src/pages/categories.ts
  </verify>
  <done>Search functionality verified - works on input</done>
</task>

</tasks>

<verification>
Code verification:
1. Check file exists: test -f src/pages/categories.ts
2. Build check: bun run build 或 build command
3. Syntax check: bun --bun tsc --noEmit

Functional verification (manual):
1. Visit http://localhost:3000/kategori
2. Check page loads with card structure
3. Search for "test" - table should filter
4. Click "+ Tambah Kategori" - modal should open
5. Fill in category name, click Simpan - should save
</verification>

<success_criteria>
- [ ] categories.ts uses card structure with card-header
- [ ] Toolbar uses .menu-toolbar with left/right sections
- [ ] Search input has 🔍 emoji in placeholder
- [ ] Table uses .table in .table-container
- [ ] Modal has proper structure
- [ ] Search/filter works
- [ ] No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-improve-ui-halaman-kategori/01-01-SUMMARY.md`
</output>